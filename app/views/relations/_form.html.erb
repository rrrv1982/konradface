<%= form_with(model: relation, local: true) do |form| %>
  <% if relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relation.errors.count, "error") %> prohibited this relation from being saved:</h2>

      <ul>
      <% relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :allowed_name %>
    <%= form.check_box :allowed_name %>
  </div>

  <div class="field">
    <%= form.label :allowed_surname %>
    <%= form.check_box :allowed_surname %>
  </div>

  <div class="field">
    <%= form.label :allowed_email %>
    <%= form.check_box :allowed_email %>
  </div>

  <div class="field">
    <%= form.label :allowed_telephone %>
    <%= form.check_box :allowed_telephone %>
  </div>

  <div class="field">
    <%= form.label :allowed_date %>
    <%= form.check_box :allowed_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
